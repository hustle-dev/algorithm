function solution(N, number) {
    let answer = 0;
    let minValue = 0;
    let dy = Array.from({length: number+1}, () => Number.MAX_SAFE_INTEGER);

    dy[0] = 0;

    return answer;
}

console.log(solution(5, 12));